import { StyleSheet } from 'react-native';

export const qrCodeDisplayStyles = StyleSheet.create({
  container: {
    alignItems: 'center',
    paddingVertical: 20,
  },
  qrIdText: {
    fontSize: 12,
    color: '#999',
    marginTop: 10,
    textAlign: 'center',
  },
});